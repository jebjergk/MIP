# Copy to `.env.local` (Vite reads this for dev). Rebuild/restart dev server after changing.
#
# Tape observer UI (polls /api/observation/tape/v1/snapshot via mip_ui_api):
VITE_TAPE_OBSERVER_ENABLED=1
